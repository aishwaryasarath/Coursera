git rm


cd checks
ls- l
git rm process.py (had to give -f to force removal)
ls -l
git status
git commit -m 'delete unnecessary process file'



git mv disk_usage.py check_free_space.py
git status
git commit -m 'New name for disk_usage.py'


there might be files that are generated by a script or added by an OS, that
don't need tracking but just add noise to git status

for that

echo .DS_STORE > .gitignore

ls -la  (all files including hidden files)
git add .gitignore
git commit -m 'Add a gitignore file, ignoring .DS_STORE'
